name: Run Tests
on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop
jobs:
  test:
    uses: zacbe/reusable-workflows/.github/workflows/nest-run-tests.yml@nest-run-tests/v1.0.0
